import {
    objectSpacing
} from "../../../../theme/tokens";

const stylesheet = {
    container: {
    },
    logo: {
        margin: objectSpacing + "px " + objectSpacing * 5 + "px"
    }
};
export default stylesheet;